[metadata]
name = gra_subjectswap
version = 0.1.0
author = GRA Team
description = Subjectness Swap Engine for human-robot interaction

[options]
packages = find:
package_dir =
    = src
install_requires =
    pyyaml
    numpy
python_requires = >=3.9

[options.packages.find]
where = src
